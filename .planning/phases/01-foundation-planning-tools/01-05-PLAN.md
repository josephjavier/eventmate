---
phase: 01-foundation-planning-tools
plan: 05
type: execute
wave: 5
depends_on: ["01-04"]
files_modified:
  - lib/checklist-template.ts
  - lib/schemas/checklist.ts
  - app/actions/checklist.ts
  - app/(client)/event/checklist/page.tsx
  - components/checklist/ChecklistView.tsx
  - components/checklist/ChecklistItemRow.tsx
  - components/checklist/AddItemDialog.tsx
  - components/checklist/OfflineSupplierDialog.tsx
  - tests/schemas/checklist.test.ts
autonomous: true
requirements: [CHECK-01, CHECK-02, CHECK-03, CHECK-04, CHECK-05, CHECK-06, CHECK-07, DISC-07]

must_haves:
  truths:
    - "A couple can load the Philippine wedding template into an empty checklist with one button"
    - "The template includes the 14 supplier categories and the PH personal-task items (CENOMAR, Pre-Cana, etc.)"
    - "A couple can add a custom item, rename it, and delete it"
    - "A supplier-task item can have an offline supplier attached (name + category required)"
    - "Supplier tasks cycle Pending -> Inquired -> Booked; personal tasks toggle Pending -> Done"
  artifacts:
    - path: "lib/checklist-template.ts"
      provides: "CHECKLIST_TEMPLATE constant: 14 categories + Personal Tasks (D-11/D-12)"
      exports: ["CHECKLIST_TEMPLATE"]
    - path: "app/actions/checklist.ts"
      provides: "loadTemplate, addItem, renameItem, deleteItem, updateStatus, attachOfflineSupplier"
      exports: ["loadTemplate", "addItem", "deleteItem"]
    - path: "app/(client)/event/checklist/page.tsx"
      provides: "Checklist page with categories and items"
  key_links:
    - from: "app/(client)/event/checklist/page.tsx"
      to: "app/actions/checklist.ts loadTemplate"
      via: "template button (D-13)"
      pattern: "loadTemplate"
    - from: "app/actions/checklist.ts"
      to: "checklist_items table"
      via: "RLS-scoped insert/update/delete"
      pattern: "checklist_items"
---

<objective>
Build the checklist vertical slice: a PH wedding template (loaded on demand, not auto-loaded), custom item add/rename/delete, supplier vs personal task types with status flows, and offline-supplier attachment (the cold-start feature — DISC-07). This is what makes the app useful before any platform supplier exists.

Purpose: Deliver the core planning value (CONTEXT.md domain): a couple can plan their entire wedding checklist, including suppliers they found off-platform.
Output: A working checklist page backed by RLS-scoped Server Actions and the hardcoded template constant.
</objective>

## Phase Goal (this slice)

**As a** couple, **I want to** work through a pre-built Philippine wedding checklist, add or remove my own items, and attach offline suppliers I found on Facebook or by referral, **so that** I get full planning value before any platform supplier is onboarded.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/01-foundation-planning-tools/01-CONTEXT.md
@.planning/phases/01-foundation-planning-tools/01-PATTERNS.md
@.planning/phases/01-foundation-planning-tools/01-UI-SPEC.md
@lib/dal.ts
@lib/supabase/server.ts
@tests/schemas/checklist.test.ts
</context>

<interfaces>
From lib/dal.ts: `verifySession()`, `assertRole('client')`
From lib/supabase/server.ts: `createClient()`
checklist_items columns (from 001_create_tables.sql): event_id, category, title, item_type ('supplier_task'|'personal_task'), status ('pending'|'inquired'|'booked'|'done'), sort_order, offline_supplier_name, offline_supplier_contact, offline_supplier_category, offline_supplier_price (INTEGER), offline_supplier_notes, booking_id
Checklist copy + status badge variants: 01-UI-SPEC.md §Checklist Page + §Checklist Status Flow (locked).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Template constant, offline-supplier schema, and checklist Server Actions</name>
  <files>lib/checklist-template.ts, lib/schemas/checklist.ts, app/actions/checklist.ts, tests/schemas/checklist.test.ts</files>
  <read_first>
    - .planning/phases/01-foundation-planning-tools/01-CONTEXT.md (D-11 14 categories in order; D-12 Personal Tasks sub-groups full item list; D-13 not auto-load; D-14 freely deletable)
    - .planning/phases/01-foundation-planning-tools/01-PATTERNS.md (§lib/checklist-template.ts pattern + TemplateItem interface; §app/actions/checklist.ts; §Shared Patterns Dual Auth)
    - .planning/phases/01-foundation-planning-tools/01-RESEARCH.md (Open Question 3 — hardcode template as TS constant; §Pattern 4 Zod)
    - tests/schemas/checklist.test.ts (failing-first DISC-07 spec)
  </read_first>
  <behavior>
    - offlineSupplierSchema: requires non-empty `name` and non-empty `category`; `contact`, `price`, `notes` optional; price (if provided) coerces to a positive number (stored as centavos by the action)
    - CHECKLIST_TEMPLATE: contains all 14 D-11 supplier categories and the D-12 personal-task items
  </behavior>
  <action>
    Create lib/checklist-template.ts exporting `interface TemplateItem { category; title; item_type; sort_order }` and `CHECKLIST_TEMPLATE: TemplateItem[]` containing: the 14 supplier categories from D-11 in order (Photography, Videography, Venue/Reception Hall, Catering, Hair & Make-up, Florist, Host/Emcee, Wedding Gown/Barong, Cake & Dessert, Invitations & Printing, Sound System, Lights & Décor, Transportation, Wedding Coordinator) each as a `supplier_task` "Book {category}" item; plus the Personal Tasks group (item_type 'personal_task') with the FULL D-12 list — Legal: Marriage license application, CENOMAR (PSA), Birth certificates (PSA), Baptismal certificates; Pre-wedding: Pre-Cana / marriage counseling seminar, Prenuptial agreement (if needed), Prenuptial photoshoot; Day-of: Prepare vows, Wedding rings, Secondary sponsors list, Entourage assignments. Sort_order ascending. Create lib/schemas/checklist.ts with `offlineSupplierSchema` and `checklistItemSchema` (title required, item_type enum); ensure tests/schemas/checklist.test.ts imports and passes. Create app/actions/checklist.ts ('use server'), each action calling `verifySession()` first and using the RLS-scoped server client: `loadTemplate(eventId)` bulk-inserts CHECKLIST_TEMPLATE rows as checklist_items for the user's event (D-13 — only on explicit call); `addItem`, `renameItem`, `deleteItem` (D-14), `updateStatus(itemId, status)` enforcing the supplier flow pending→inquired→booked and personal flow pending→done, `attachOfflineSupplier(itemId, data)` validating offlineSupplierSchema and converting price via phpToCentavos. revalidatePath('/event/checklist') after mutations.
  </action>
  <verify>
    <automated>npx vitest run tests/schemas/checklist.test.ts && grep -q "CENOMAR" lib/checklist-template.ts && grep -q "Pre-Cana" lib/checklist-template.ts && grep -c "category:" lib/checklist-template.ts | grep -qE "[2-9][0-9]" && grep -q "verifySession" app/actions/checklist.ts && npx tsc --noEmit && echo CHECKLIST_ACTIONS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/schemas/checklist.test.ts` exits 0
    - `lib/checklist-template.ts` contains all 14 D-11 categories and the D-12 items including "CENOMAR (PSA)" and "Pre-Cana / marriage counseling seminar"
    - `app/actions/checklist.ts` exports `loadTemplate`, `addItem`, `renameItem`, `deleteItem`, `updateStatus`, `attachOfflineSupplier`; each calls `verifySession()`
    - `attachOfflineSupplier` rejects when name or category is empty and stores price as centavos
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Template constant, offline-supplier validation, and all checklist Server Actions implemented with dual-auth and centavos handling.</done>
</task>

<task type="auto">
  <name>Task 2: Checklist page + components (template load, items, status, offline supplier)</name>
  <files>app/(client)/event/checklist/page.tsx, components/checklist/ChecklistView.tsx, components/checklist/ChecklistItemRow.tsx, components/checklist/AddItemDialog.tsx, components/checklist/OfflineSupplierDialog.tsx</files>
  <read_first>
    - .planning/phases/01-foundation-planning-tools/01-UI-SPEC.md (§Checklist Page copy — all locked strings incl. delete-confirm copy; §Page-Level Layout Contracts: Checklist Page; §Interaction Contracts: Checklist Status Flow badge variants; §Accessibility icon-only buttons)
    - .planning/phases/01-foundation-planning-tools/01-PATTERNS.md (§No Analog Found — checklist page derive-from; shadcn Checkbox/Collapsible)
    - .planning/phases/01-foundation-planning-tools/01-CONTEXT.md (D-13 template button label exact; D-14)
    - app/actions/checklist.ts (Task 1 server actions)
  </read_first>
  <action>
    Create app/(client)/event/checklist/page.tsx (Server Component): `assertRole('client')`, load the event + checklist_items grouped by category. Empty state (no items) renders the exact UI-SPEC copy "Your checklist is empty" / body, the "Start from the Philippine wedding template" button (D-13 exact label) wired to `loadTemplate`, and "Add Item". Non-empty: render ChecklistView. components/checklist/ChecklistView.tsx ('use client'): categories as collapsible groups (shadcn Collapsible/Card) each showing name, item count, completion badge, and an "Add Item" trigger. components/checklist/ChecklistItemRow.tsx: per-item row — personal_task shows a Checkbox toggling pending↔done; supplier_task shows a status Badge cycling pending→inquired→booked with the locked variant styles (Pending outline, Inquired secondary, Booked accent fill) and an action menu (Rename, Attach/Edit Supplier, Delete) — icon-only buttons need aria-labels. Delete uses a confirm Dialog with the exact UI-SPEC delete copy. AddItemDialog.tsx: form (title + item_type) → addItem; uses sonner toast on success. OfflineSupplierDialog.tsx: form with the exact UI-SPEC "Add a Supplier" heading/body/labels → attachOfflineSupplier (name + category required, optional contact/price/notes). All currency display via formatPHP. Use only weights 400/600 and stone/accent palette.
  </action>
  <verify>
    <automated>grep -q "Start from the Philippine wedding template" app/\(client\)/event/checklist/page.tsx && grep -q "assertRole" app/\(client\)/event/checklist/page.tsx && grep -q "loadTemplate" app/\(client\)/event/checklist/page.tsx && grep -rq "aria-label" components/checklist/ && npx tsc --noEmit && echo CHECKLIST_UI_OK</automated>
  </verify>
  <acceptance_criteria>
    - Empty checklist renders the exact D-13 button label "Start from the Philippine wedding template"
    - Page calls `assertRole('client')` and wires `loadTemplate`, `addItem`, `deleteItem`, `attachOfflineSupplier`
    - Supplier-task status badges use the locked variants (Pending outline / Inquired secondary / Booked accent)
    - Offline-supplier dialog uses the exact UI-SPEC "Add a Supplier" copy and requires name + category
    - Icon-only action buttons have `aria-label`s
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Checklist page renders template load, both item types with correct status flows, and offline-supplier attachment with locked copy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → checklist Server Actions | All item writes must be event-scoped via RLS + DAL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Information Disclosure | Cross-event checklist access | mitigate | Every action calls `verifySession()`; checklist_items RLS gated on `user_can_access_event(event_id)` |
| T-1-08 | Tampering | Offline supplier input / price | mitigate | `offlineSupplierSchema` (Zod) validates server-side; price stored via `phpToCentavos` (INTEGER) |
| T-1-10 | Tampering | Invalid status transition | mitigate | `updateStatus` enforces the allowed status set per item_type; DB CHECK constraint backs it |
</threat_model>

<verification>
- `npx vitest run tests/schemas/checklist.test.ts` green
- Template constant contains all D-11/D-12 items
- Page renders exact D-13 button label; actions call verifySession
- `npx tsc --noEmit` exits 0
</verification>

<success_criteria>
- ROADMAP Success Criterion 2 satisfied: PH template, custom items, offline supplier attachment
- Both item types with correct status flows
- All checklist copy matches UI-SPEC
</success_criteria>

<output>
Create `.planning/phases/01-foundation-planning-tools/01-05-SUMMARY.md` when done.
</output>

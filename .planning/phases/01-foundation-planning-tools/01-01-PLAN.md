---
phase: 01-foundation-planning-tools
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.ts
  - tsconfig.json
  - components.json
  - app/globals.css
  - app/layout.tsx
  - app/page.tsx
  - vitest.config.ts
  - vitest.setup.ts
  - tests/utils/currency.test.ts
  - tests/utils/token.test.ts
  - tests/utils/rsvp.test.ts
  - tests/schemas/auth.test.ts
  - tests/schemas/checklist.test.ts
  - .env.local.example
  - .gitignore
autonomous: false
requirements: [AUTH-01, BUDG-01, RSVP-03, DISC-07]
user_setup:
  - service: supabase
    why: "Database, Auth, Storage — project must exist before Wave 2 schema push"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard -> Project Settings -> API -> Project URL"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> anon/public key"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> service_role key (NEVER expose to browser)"
    dashboard_config:
      - task: "Create a new Supabase project (free tier)"
        location: "https://dashboard.supabase.com"

must_haves:
  truths:
    - "Running `npm run dev` starts a Next.js 15 app with no build errors"
    - "Running `npx vitest run` executes and reports the Wave-0 test files (red until implementations land in later waves)"
    - "package.json pins next to 15.x (not 16.x)"
  artifacts:
    - path: "package.json"
      provides: "Next.js 15.3.9 + all Phase 1 dependencies pinned"
      contains: "\"next\": \"15"
    - path: "vitest.config.ts"
      provides: "Vitest test runner config with @ path alias and jsdom env"
    - path: "components.json"
      provides: "shadcn/ui initialized with stone base color"
    - path: "tests/utils/currency.test.ts"
      provides: "Failing-first tests for BUDG-01 centavos arithmetic"
  key_links:
    - from: "vitest.config.ts"
      to: "lib/utils.ts (created Wave 3)"
      via: "@ path alias resolution"
      pattern: "alias.*@"
---

<objective>
Scaffold the greenfield EventMate project: bootstrap Next.js 15.3.9 (pinned), run the async-API codemod, initialize shadcn/ui, install all Phase 1 dependencies, configure the Vitest harness, and create the five Wave-0 test files as failing-first specs. This is the foundation every other plan builds on.

Purpose: Establish a deployable, test-runnable Next.js 15 codebase with the exact verified dependency versions from RESEARCH.md. No feature code yet — just a clean, correctly-versioned skeleton base.
Output: A project that runs `npm run dev` and `npx vitest run` successfully, with shadcn/ui ready and all dependencies installed.
</objective>

## Phase Goal (this slice)

**As a** solo developer, **I want to** stand up a correctly-versioned Next.js 15 + Supabase + shadcn/ui project with a working test harness, **so that** every feature slice is built on a stable, verified foundation instead of mismatched library versions.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/01-foundation-planning-tools/01-RESEARCH.md
@.planning/phases/01-foundation-planning-tools/01-UI-SPEC.md
@.planning/phases/01-foundation-planning-tools/01-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify package legitimacy before install (supply-chain gate)</name>
  <read_first>
    - .planning/phases/01-foundation-planning-tools/01-RESEARCH.md (§Package Legitimacy Audit — all packages tagged [ASSUMED] because slopcheck was unavailable)
  </read_first>
  <action>
    BLOCKING supply-chain gate that runs BEFORE any `npm install`. Pause and have the developer confirm every package in RESEARCH.md §Standard Stack Installation against its official npmjs.com page (registry existence + official repo, no typosquats) per the steps below. Do not run any install until "approved".
  </action>
  <what-built>
    Nothing yet — this gate runs BEFORE any `npm install`. RESEARCH.md §Package Legitimacy Audit lists every package as [ASSUMED] because slopcheck was unavailable at research time. Per the supply-chain threat (T-1-SC), every install must be confirmed against the public registry before running.
  </what-built>
  <how-to-verify>
    For each package in RESEARCH.md §Standard Stack Installation, confirm it exists and is the official package by opening its npmjs.com page:
    1. next (vercel), @supabase/supabase-js, @supabase/ssr (supabase), react-hook-form, zod, @hookform/resolvers, @tanstack/react-query, @tanstack/react-table, zustand, date-fns, nanoid, resend (resend), @react-email/components, react-dropzone, sonner, lucide-react
    2. Confirm each "Repository" link points to the official GitHub org listed in the audit table.
    3. Confirm no typosquat (e.g., `@supabase/ssr` NOT `@supabase-ssr`).
  </how-to-verify>
  <acceptance_criteria>
    - Developer confirms all 16 packages match the official repos in RESEARCH.md §Package Legitimacy Audit
    - No package flagged [SLOP] or [SUS] proceeds to install
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed with installs, or name any package to investigate.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Bootstrap Next.js 15.3.9, run codemod, init shadcn/ui, install deps</name>
  <files>package.json, next.config.ts, tsconfig.json, components.json, app/globals.css, app/layout.tsx, app/page.tsx, .gitignore, .env.local.example</files>
  <read_first>
    - .planning/phases/01-foundation-planning-tools/01-RESEARCH.md (§Standard Stack Installation — exact commands and pinned versions; §Common Pitfalls 1, 2, 8)
    - .planning/phases/01-foundation-planning-tools/01-UI-SPEC.md (§Design System — shadcn init preset; §Typography — Plus Jakarta Sans; §Color — stone base)
    - CLAUDE.md (§Stack)
  </read_first>
  <action>
    The project must be scaffolded INTO the current directory (already a git repo with .planning/). Run `npx create-next-app@15.3.9 .` with flags `--typescript --tailwind --app --eslint --no-src-dir --import-alias "@/*"` (accept into the existing non-empty dir; preserve .planning/, CLAUDE.md, idea.md, .git). Do NOT use `@latest` — it installs Next.js 16 which renames middleware.ts to proxy.ts (Pitfall 1). Immediately run the async-API codemod `npx @next/codemod@canary next-async-request-api .` (Pitfall 8 — cookies/params/searchParams are async in Next 15). Initialize shadcn/ui with `npx shadcn@latest init --base-color stone --css-variables` (UI-SPEC preset: default style, stone, CSS variables). Add the Phase 1 shadcn components: button input label form card dialog sheet select textarea badge tabs separator avatar dropdown-menu calendar progress alert checkbox popover command. Install all npm deps exactly as RESEARCH.md §Installation Step 4 lists (verified versions): @supabase/supabase-js @supabase/ssr react-hook-form zod @hookform/resolvers @tanstack/react-query @tanstack/react-table zustand date-fns nanoid resend @react-email/components react-dropzone sonner; and dev deps: supabase (CLI). Configure Plus Jakarta Sans via `next/font/google` (weights 400, 600 only) in app/layout.tsx and wire `--font-sans` per UI-SPEC §Typography. Add `<Toaster />` from sonner to the root layout. Create app/page.tsx as a minimal landing page that links to /login and /signup. Create .env.local.example documenting NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, RESEND_API_KEY, NEXT_PUBLIC_SITE_URL (SUPABASE_SERVICE_ROLE_KEY must NOT have NEXT_PUBLIC_ prefix). Ensure .gitignore includes .env.local and .env*.local.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q '"next": "15' package.json && test -f components.json && echo SCAFFOLD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"next": "15` (NOT 16.x)
    - `components.json` exists (shadcn initialized)
    - `app/layout.tsx` imports Plus_Jakarta_Sans from next/font/google with weights 400 and 600
    - `app/layout.tsx` renders the sonner `<Toaster />`
    - `.env.local.example` contains SUPABASE_SERVICE_ROLE_KEY with NO `NEXT_PUBLIC_` prefix
    - `npx tsc --noEmit` exits 0
    - `npm run dev` starts without errors (manual spot check)
  </acceptance_criteria>
  <done>Next.js 15.3.9 app builds, shadcn/ui initialized with stone base, all dependencies installed, font + Toaster wired, env example documented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Configure Vitest and write the five Wave-0 failing-first test files</name>
  <files>vitest.config.ts, vitest.setup.ts, tests/utils/currency.test.ts, tests/utils/token.test.ts, tests/utils/rsvp.test.ts, tests/schemas/auth.test.ts, tests/schemas/checklist.test.ts, package.json</files>
  <read_first>
    - .planning/phases/01-foundation-planning-tools/01-VALIDATION.md (§Per-Task Verification Map, §Wave 0 Requirements — exact test files and commands)
    - .planning/phases/01-foundation-planning-tools/01-RESEARCH.md (§Validation Architecture — Phase Requirements → Test Map; §Pattern 5 currency signatures; Open Question 3 checklist template)
    - .planning/phases/01-foundation-planning-tools/01-PATTERNS.md (§vitest.config.ts + Test Files — test pattern example)
  </read_first>
  <behavior>
    - tests/utils/currency.test.ts: `phpToCentavos(50000)` === `5000000`; `centavosToPhp(5000000)` === `50000`; `formatPHP(5000000)` matches `/50,000/` (BUDG-01)
    - tests/utils/token.test.ts: generated RSVP token has length 20 and matches `/^[A-Za-z0-9_-]{20}$/` (RSVP-03)
    - tests/utils/rsvp.test.ts: deadline gate returns locked=true when `rsvp_deadline` < today, locked=false when >= today (D-06 / RSVP-04)
    - tests/schemas/auth.test.ts: signup schema rejects password shorter than 8 chars and requires `consent === true` (AUTH-01 + RA 10173)
    - tests/schemas/checklist.test.ts: offline supplier schema requires non-empty name and category (DISC-07)
  </behavior>
  <action>
    Install dev deps `vitest @testing-library/react @testing-library/jest-dom jsdom @vitejs/plugin-react`. Create vitest.config.ts with the `@vitejs/plugin-react` plugin, `test.environment = 'jsdom'`, `test.globals = true`, `test.setupFiles = ['./vitest.setup.ts']`, and a resolve alias mapping `@` to the project root so imports like `@/lib/utils` resolve (matches tsconfig paths). Create vitest.setup.ts importing `@testing-library/jest-dom`. Add `"test": "vitest run"` and `"test:watch": "vitest"` scripts to package.json. Write the five test files per the behaviors above, importing from the not-yet-created modules (`@/lib/utils`, `@/lib/utils` token helper, `@/lib/rsvp`, `@/lib/schemas/auth`, `@/lib/schemas/checklist`). These tests are EXPECTED to fail/red now (modules created in Waves 3/5/6/8) — this is the Nyquist failing-first scaffold. Do NOT implement the modules here. The token helper and rsvp deadline helper signatures must match what Wave 3 / Wave 8 will export: `generateRsvpToken(): string` and `isRsvpDeadlinePassed(deadline: string | null, now?: Date): boolean`.
  </action>
  <verify>
    <automated>npx vitest run --reporter=basic 2>&1 | grep -E "(Test Files|tests/)" || echo "vitest executed (red expected)"</automated>
  </verify>
  <acceptance_criteria>
    - `vitest.config.ts` exists with `environment: 'jsdom'` and an `@` resolve alias
    - All five test files exist under `tests/`
    - `npx vitest run` executes and discovers the test files (failures are acceptable — implementations land in later waves)
    - package.json has a `test` script running `vitest run` (no `--watch`)
  </acceptance_criteria>
  <done>Vitest harness configured; five Wave-0 test specs written as failing-first contracts that later waves must satisfy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local machine | Package tarballs are executed during install (postinstall scripts) — untrusted code crosses here |
| developer machine → git | Secrets must not be committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-SC | Tampering | npm installs (16 packages, all [ASSUMED]) | mitigate | Task 1 blocking-human checkpoint verifies every package against official npmjs.com repo before any install runs; slopcheck unavailable so manual gate is mandatory |
| T-1-EV | Information Disclosure | .env.local secrets | mitigate | `.gitignore` includes `.env.local` and `.env*.local`; only `.env.local.example` (placeholders) is committed; SUPABASE_SERVICE_ROLE_KEY never `NEXT_PUBLIC_` |
| T-1-VER | Tampering | Next.js version drift | mitigate | Pin `create-next-app@15.3.9`; acceptance criterion asserts `"next": "15` in package.json (Pitfall 1) |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `grep '"next": "15' package.json` matches
- `npx vitest run` discovers and runs the five Wave-0 test files
- `components.json` exists
- `.env.local` is git-ignored; `.env.local.example` is committed
</verification>

<success_criteria>
- Next.js 15.3.9 project bootstrapped in-place, preserving `.planning/`, `CLAUDE.md`, `idea.md`, `.git`
- shadcn/ui initialized (stone, CSS variables) with all 20 Phase 1 components added
- All RESEARCH.md-verified dependencies installed at the documented versions
- Vitest harness runs; five Wave-0 failing-first tests authored
- Package supply-chain gate cleared by the developer
</success_criteria>

<output>
Create `.planning/phases/01-foundation-planning-tools/01-01-SUMMARY.md` when done.
</output>

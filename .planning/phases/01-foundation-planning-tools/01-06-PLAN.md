---
phase: 01-foundation-planning-tools
plan: 06
type: execute
wave: 5
depends_on: ["01-04"]
files_modified:
  - lib/schemas/budget.ts
  - app/actions/budget.ts
  - app/(client)/event/budget/page.tsx
  - components/budget/CurrencyInput.tsx
  - components/budget/BudgetSummary.tsx
  - components/budget/CategoryList.tsx
  - components/budget/CategoryAllocationDialog.tsx
  - components/budget/ExpenseDialog.tsx
  - tests/utils/budget.test.ts
autonomous: true
requirements: [BUDG-01, BUDG-02, BUDG-03, BUDG-04, BUDG-05, BUDG-06]

must_haves:
  truths:
    - "A couple can set a total wedding budget in pesos (stored as INTEGER centavos)"
    - "A couple can allocate a budget amount per category"
    - "Each expense records total price, deposit paid (amount+date), remaining balance, and balance due date"
    - "A couple can attach a receipt (PDF/JPG/PNG) to an expense"
    - "The budget summary shows total / committed / paid / remaining"
    - "A category that exceeds its allocation shows an over-budget alert"
  artifacts:
    - path: "app/actions/budget.ts"
      provides: "setBudget, upsertCategory, createExpense, updateExpense, deleteExpense, attachReceipt"
      exports: ["createExpense", "setBudget"]
    - path: "components/budget/CurrencyInput.tsx"
      provides: "PHP input that stores centavos"
    - path: "components/budget/CategoryAllocationDialog.tsx"
      provides: "Dialog to set/edit a category's allocated_amount, calls upsertCategory"
    - path: "app/(client)/event/budget/page.tsx"
      provides: "Budget page: summary + categories + expenses"
  key_links:
    - from: "app/actions/budget.ts createExpense"
      to: "expenses table (INTEGER centavos)"
      via: "phpToCentavos before insert"
      pattern: "phpToCentavos"
    - from: "components/budget/CategoryList.tsx"
      to: "over-budget comparison"
      via: "SUM(expenses) vs allocated_amount"
      pattern: "Over budget"
    - from: "components/budget/CategoryAllocationDialog.tsx"
      to: "app/actions/budget.ts upsertCategory"
      via: "form submit → phpToCentavos(input) → upsertCategory"
      pattern: "upsertCategory"
---

<objective>
Build the budget vertical slice (GCash/wallet-inspired): set a total budget, allocate per category, track expenses with deposit/balance/due-date, attach receipts, see a 4-stat summary, and get a visual over-budget alert. Every monetary value is INTEGER centavos end-to-end.

Purpose: Deliver familiar, accurate budget tracking for Filipino users at ₱500k+ scales without rounding errors (CLAUDE.md decision #1; CONTEXT.md budget UX inspiration).
Output: A working budget page with centavos-correct storage and over-budget alerts.
</objective>

## Phase Goal (this slice)

**As a** couple, **I want to** set a total budget, allocate it per category, record each supplier's deposit and remaining balance, and be alerted when a category goes over, **so that** I never lose track of where our wedding money is going.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/01-foundation-planning-tools/01-PATTERNS.md
@.planning/phases/01-foundation-planning-tools/01-UI-SPEC.md
@.planning/phases/01-foundation-planning-tools/01-CONTEXT.md
@lib/dal.ts
@lib/utils.ts
@tests/utils/currency.test.ts
</context>

<interfaces>
From lib/utils.ts: `phpToCentavos(n)`, `centavosToPhp(c)`, `formatPHP(c)`
From lib/dal.ts: `verifySession()`, `assertRole('client')`
expenses columns: event_id, checklist_item_id (nullable), supplier_name, total_amount (INTEGER), deposit_paid (INTEGER), deposit_paid_date, remaining_balance (INTEGER), balance_due_date
budget_categories columns: event_id, category, allocated_amount (INTEGER)
receipt_files columns: expense_id, storage_path, file_name, mime_type
Budget copy + 4-stat layout + over-budget badge: 01-UI-SPEC.md §Budget Page + §Interaction Contracts (Currency Inputs, Budget Over-Limit Alert).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Budget schemas + Server Actions (centavos-correct)</name>
  <files>lib/schemas/budget.ts, app/actions/budget.ts, tests/utils/budget.test.ts</files>
  <read_first>
    - .planning/phases/01-foundation-planning-tools/01-PATTERNS.md (§app/actions/budget.ts full pattern — centavos conversion + remaining = total - deposit; §Shared Patterns: INTEGER Centavos)
    - .planning/phases/01-foundation-planning-tools/01-RESEARCH.md (§Code Examples: Currency Input → DB Storage; §Pattern 4 Zod; §Pitfall 3 INTEGER)
    - .planning/phases/01-foundation-planning-tools/01-CONTEXT.md (BUDG decisions; budget UX inspiration)
    - lib/utils.ts (phpToCentavos/centavosToPhp/formatPHP)
  </read_first>
  <behavior>
    - createExpense computes `remaining_balance = total_amount - deposit_paid` in centavos
    - over-budget helper `isOverBudget(spentCentavos, allocatedCentavos)` returns true when spent > allocated
    - all money inputs validated as non-negative numbers, stored as INTEGER centavos
  </behavior>
  <action>
    Create lib/schemas/budget.ts (Zod v4): `setBudgetSchema` (total_budget number >= 0), `categorySchema` (category string, allocated number >= 0), `expenseSchema` (supplier_name, total_amount number >= 0, deposit_paid number >= 0, deposit_paid_date optional, balance_due_date optional, checklist_item_id optional). Also export a pure `isOverBudget(spent: number, allocated: number): boolean` used by both UI and a unit test. Create tests/utils/budget.test.ts asserting the centavos math (e.g., total 5000000 - deposit 1500000 = 3500000) and `isOverBudget`. Create app/actions/budget.ts ('use server'), each action `verifySession()` first: `setBudget(eventId, php)` updates events.total_budget = phpToCentavos(php); `upsertCategory(eventId, category, allocatedPhp)` writes budget_categories.allocated_amount as phpToCentavos(allocatedPhp) — INTEGER centavos, upsert on (event_id, category); `createExpense(formData)` per PATTERNS.md — convert total + deposit via Math.round(x*100), remaining = total - deposit, insert into expenses; `updateExpense`, `deleteExpense`; `attachReceipt(expenseId, storagePath, fileName, mimeType)` inserts receipt_files (validate mime in pdf/jpeg/png). revalidatePath('/event/budget').
  </action>
  <verify>
    <automated>npx vitest run tests/utils/budget.test.ts && grep -q "phpToCentavos\|Math.round" app/actions/budget.ts && grep -q "remaining" app/actions/budget.ts && grep -q "verifySession" app/actions/budget.ts && npx tsc --noEmit && echo BUDGET_ACTIONS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/utils/budget.test.ts` exits 0 (centavos math + isOverBudget)
    - `app/actions/budget.ts` exports `setBudget`, `upsertCategory`, `createExpense`, `updateExpense`, `deleteExpense`, `attachReceipt`; each calls `verifySession()`
    - `createExpense` stores `remaining_balance = total - deposit` in centavos and never stores a float
    - `upsertCategory` stores `allocated_amount` as `phpToCentavos(allocatedPhp)` — INTEGER centavos
    - `attachReceipt` rejects mime types outside pdf/jpeg/png
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Budget schemas and centavos-correct Server Actions implemented with passing math tests.</done>
</task>

<task type="auto">
  <name>Task 2: Budget page + components (summary, categories, allocation dialog, expense dialog, over-budget alert)</name>
  <files>app/(client)/event/budget/page.tsx, components/budget/CurrencyInput.tsx, components/budget/BudgetSummary.tsx, components/budget/CategoryList.tsx, components/budget/CategoryAllocationDialog.tsx, components/budget/ExpenseDialog.tsx</files>
  <read_first>
    - .planning/phases/01-foundation-planning-tools/01-UI-SPEC.md (§Budget Page copy — all locked strings; §Page-Level Layout Contracts: Budget Page GCash-inspired 4 stat cards; §Interaction Contracts: Currency Inputs + Budget Over-Limit Alert)
    - .planning/phases/01-foundation-planning-tools/01-PATTERNS.md (§components/budget/CurrencyInput.tsx pattern)
    - app/actions/budget.ts (Task 1)
    - lib/utils.ts (formatPHP)
  </read_first>
  <action>
    Create components/budget/CurrencyInput.tsx ('use client'): text input with inputMode="numeric", placeholder "e.g., ₱ 50,000", formats on blur via Intl.NumberFormat en-PH (no decimals for whole pesos), emits a numeric peso value the parent converts via phpToCentavos on submit. Create components/budget/CategoryAllocationDialog.tsx ('use client'): a shadcn Dialog containing a form with a single CurrencyInput labeled "Allocated amount (₱)" and a submit button labeled "Save". The form calls `upsertCategory(eventId, category, phpToCentavos(input))` on submit — the phpToCentavos conversion MUST happen before storing so allocated_amount is always INTEGER centavos (BUDG-02). The dialog accepts `eventId`, `category`, and the current `allocatedAmount` (centavos) as props; it converts to PHP via `centavosToPhp` for the initial display value. On success show a sonner toast "Allocation updated" and close the dialog. Create app/(client)/event/budget/page.tsx (Server Component): `assertRole('client')`, load event.total_budget, budget_categories, and expenses (with SUM per category). Empty state (no budget) renders exact copy "No budget set yet" + "Set Budget" CTA. With budget: render BudgetSummary + CategoryList + an "Add Expense" trigger. BudgetSummary.tsx: 4 stat cards in a row (Total Budget / Committed / Paid / Remaining) per UI-SPEC labels, each a large number via formatPHP — Committed = SUM(expenses.total_amount), Paid = SUM(deposit_paid), Remaining = total_budget - committed (BUDG-05). CategoryList.tsx: each row shows category name + allocated (formatPHP) + spent (formatPHP) + a thin progress bar (accent when under, destructive when over) + an "Edit" button that opens `CategoryAllocationDialog` for that category (BUDG-02); when SUM(expenses for category) > allocated_amount render the destructive Badge "Over budget by ₱X,XXX" inline (BUDG-06, informational, non-blocking). ExpenseDialog.tsx ('use client'): form with the exact UI-SPEC expense labels (total package price, deposit paid, date paid, remaining balance auto-derived, balance due date) using CurrencyInput → createExpense; include an "Attach Receipt" action (react-dropzone, PDF/JPG/PNG) that uploads to the `receipts` bucket under the event-id folder then calls attachReceipt. sonner toasts on mutations. Weights 400/600, stone/accent/destructive palette only.
  </action>
  <verify>
    <automated>grep -q "Over budget" components/budget/CategoryList.tsx && grep -q "CategoryAllocationDialog" components/budget/CategoryList.tsx && grep -q "upsertCategory" components/budget/CategoryAllocationDialog.tsx && grep -q "phpToCentavos" components/budget/CategoryAllocationDialog.tsx && grep -q "assertRole" app/\(client\)/event/budget/page.tsx && grep -q "inputMode" components/budget/CurrencyInput.tsx && grep -q "formatPHP" components/budget/BudgetSummary.tsx && npx tsc --noEmit && echo BUDGET_UI_OK</automated>
  </verify>
  <acceptance_criteria>
    - BudgetSummary renders the four stat cards with exact UI-SPEC labels (Total Budget / Committed / Paid / Remaining) via `formatPHP`
    - CategoryList renders an "Edit" button per row that opens `CategoryAllocationDialog` for that category (BUDG-02)
    - CategoryAllocationDialog shows a CurrencyInput for the allocated amount; submitting calls `upsertCategory` which stores `phpToCentavos(input)` as INTEGER centavos
    - CategoryList renders the destructive "Over budget by ₱X,XXX" badge when spent exceeds allocation
    - CurrencyInput uses `inputMode="numeric"` and the exact placeholder; never displays decimals for whole pesos
    - Expense dialog uses exact UI-SPEC labels and supports receipt upload (PDF/JPG/PNG) to the receipts bucket
    - Page calls `assertRole('client')`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Budget page renders the 4-stat summary, per-category allocation rows with an Edit button that opens CategoryAllocationDialog (BUDG-02), over-budget alerts, and expense entry with receipt attachment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → budget Server Actions | Event-scoped money writes via RLS + DAL |
| client → receipts bucket | Upload gated by storage RLS (event-id folder) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-03 | Tampering | Float money / rounding | mitigate | All amounts converted via `phpToCentavos`/Math.round to INTEGER centavos; unit test asserts exact arithmetic |
| T-1-01 | Information Disclosure | Cross-event budget access | mitigate | `verifySession()` in every action; expenses/budget_categories RLS gated on `user_can_access_event` |
| T-1-11 | Tampering | Malicious receipt upload | mitigate | Client (react-dropzone accept) + server `attachReceipt` mime check restrict to pdf/jpeg/png; storage RLS scopes to event folder |
</threat_model>

<verification>
- `npx vitest run tests/utils/budget.test.ts` green
- Over-budget badge present; CurrencyInput numeric; actions call verifySession
- CategoryList "Edit" button per row opens CategoryAllocationDialog; dialog calls upsertCategory with phpToCentavos(input)
- `npx tsc --noEmit` exits 0
</verification>

<success_criteria>
- ROADMAP Success Criterion 3 satisfied: total budget, per-category allocation with edit UI, deposit/balance/due-date, visual over-budget alert
- All money stored as INTEGER centavos
- Budget copy matches UI-SPEC
</success_criteria>

<output>
Create `.planning/phases/01-foundation-planning-tools/01-06-SUMMARY.md` when done.
</output>

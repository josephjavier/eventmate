---
phase: 01-foundation-planning-tools
plan: 07
type: execute
wave: 5
depends_on: ["01-04"]
files_modified:
  - lib/schemas/file.ts
  - app/actions/files.ts
  - app/(client)/event/files/page.tsx
  - components/files/FileDropzone.tsx
  - components/files/FileGrid.tsx
  - components/files/FileCard.tsx
  - tests/schemas/file.test.ts
autonomous: true
requirements: [FILE-01, FILE-02, FILE-03, FILE-04]

must_haves:
  truths:
    - "A couple can upload a PDF, JPG, or PNG tagged to a supplier or checklist item"
    - "A couple can upload a general event file not tied to a supplier"
    - "A couple can view and download any uploaded file via a signed URL"
    - "Non-PDF/JPG/PNG files are rejected before upload"
  artifacts:
    - path: "app/actions/files.ts"
      provides: "saveFilePath, getSignedUrl, deleteFile (server-side mime re-check)"
      exports: ["saveFilePath", "getSignedUrl", "deleteFile"]
    - path: "components/files/FileDropzone.tsx"
      provides: "react-dropzone with PDF/JPG/PNG accept + 10MB limit"
    - path: "app/(client)/event/files/page.tsx"
      provides: "Files page: dropzone + grid"
  key_links:
    - from: "components/files/FileDropzone.tsx"
      to: "event-files bucket"
      via: "Supabase Storage upload (event-id folder) then saveFilePath"
      pattern: "event-files"
    - from: "components/files/FileCard.tsx"
      to: "app/actions/files.ts getSignedUrl"
      via: "download button"
      pattern: "getSignedUrl"
---

<objective>
Build the file storage vertical slice: drag-and-drop upload of PDF/JPG/PNG (tagged to a supplier/checklist item or general), a file grid, and signed-URL downloads — all backed by the private `event-files` bucket with RLS. MIME type is validated client-side AND server-side.

Purpose: Keep every contract, receipt, and quotation in one place (core value statement), with private storage that only event participants can access.
Output: A working files page with secure upload/download.
</objective>

## Phase Goal (this slice)

**As a** couple, **I want to** upload our contracts, receipts, and quotations (PDF/JPG/PNG) and download any of them anytime, **so that** all our wedding documents live in one secure place.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/01-foundation-planning-tools/01-PATTERNS.md
@.planning/phases/01-foundation-planning-tools/01-UI-SPEC.md
@.planning/phases/01-foundation-planning-tools/01-RESEARCH.md
@lib/dal.ts
@lib/supabase/server.ts
</context>

<interfaces>
From lib/dal.ts: `verifySession()`, `assertRole('client')`
From lib/supabase/server.ts: `createClient()`
event_files columns: event_id, checklist_item_id (nullable), supplier_label, storage_path, file_name, mime_type CHECK in ('application/pdf','image/jpeg','image/png')
Storage convention (from 002_storage_buckets.sql): object name = `{event_id}/{filename}`; bucket `event-files` is private.
Files copy + dropzone contract: 01-UI-SPEC.md §Files Page + §Interaction Contracts: File Upload Flow + §Global Error States.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: File schema + Server Actions (mime re-check, signed URLs)</name>
  <files>lib/schemas/file.ts, app/actions/files.ts, tests/schemas/file.test.ts</files>
  <read_first>
    - .planning/phases/01-foundation-planning-tools/01-RESEARCH.md (§Architecture Responsibility Map — file upload row: client uploads to Storage, Server Action saves path + generates signed URL; §Security Domain — file upload abuse, validate mime both sides; FILE-04)
    - .planning/phases/01-foundation-planning-tools/01-PATTERNS.md (§app/actions/files.ts role; §Shared Patterns Dual Auth)
    - CLAUDE.md (RLS, service role rules)
  </read_first>
  <behavior>
    - file schema: accepts only mime in {application/pdf, image/jpeg, image/png}; rejects others
    - file schema: rejects size > 10 MB
  </behavior>
  <action>
    Create lib/schemas/file.ts exporting `ACCEPTED_MIME = ['application/pdf','image/jpeg','image/png']`, `MAX_FILE_BYTES = 10*1024*1024`, and `fileMetaSchema` (file_name string, mime_type refined to ACCEPTED_MIME, size <= MAX_FILE_BYTES, checklist_item_id optional, supplier_label optional). Create tests/schemas/file.test.ts asserting a .pdf/.jpg/.png passes and e.g. video/mp4 or an 11MB file fails. Create app/actions/files.ts ('use server'), each action `verifySession()` first: `saveFilePath(eventId, { storage_path, file_name, mime_type, checklist_item_id?, supplier_label? })` — server-side re-validate mime against ACCEPTED_MIME (defense in depth; do not trust the client) then insert into event_files; `getSignedUrl(fileId)` — look up the row (RLS-scoped), call `supabase.storage.from('event-files').createSignedUrl(path, 60)` and return the URL (FILE-03); `deleteFile(fileId)` — remove the storage object and the row. revalidatePath('/event/files').
  </action>
  <verify>
    <automated>npx vitest run tests/schemas/file.test.ts && grep -q "application/pdf" lib/schemas/file.ts && grep -q "createSignedUrl" app/actions/files.ts && grep -q "verifySession" app/actions/files.ts && npx tsc --noEmit && echo FILES_ACTIONS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/schemas/file.test.ts` exits 0 (accepts pdf/jpg/png; rejects others and >10MB)
    - `app/actions/files.ts` exports `saveFilePath`, `getSignedUrl`, `deleteFile`; each calls `verifySession()`
    - `saveFilePath` re-checks mime server-side against `ACCEPTED_MIME`
    - `getSignedUrl` uses `createSignedUrl` on the private `event-files` bucket
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>File metadata schema and Server Actions implemented with server-side mime re-validation and signed-URL downloads.</done>
</task>

<task type="auto">
  <name>Task 2: Files page + dropzone + grid (tagging, download, delete)</name>
  <files>app/(client)/event/files/page.tsx, components/files/FileDropzone.tsx, components/files/FileGrid.tsx, components/files/FileCard.tsx</files>
  <read_first>
    - .planning/phases/01-foundation-planning-tools/01-UI-SPEC.md (§Files Page copy — all locked strings; §Page-Level Layout Contracts: Files Page 96px dropzone, 3-col grid; §Interaction Contracts: File Upload Flow; §Global Error States; §Accessibility icon-only buttons)
    - .planning/phases/01-foundation-planning-tools/01-PATTERNS.md (§No Analog Found — files page derive-from: react-dropzone + Supabase signed URL)
    - app/actions/files.ts (Task 1)
    - lib/schemas/file.ts (ACCEPTED_MIME, MAX_FILE_BYTES)
  </read_first>
  <action>
    Create components/files/FileDropzone.tsx ('use client'): react-dropzone with `accept` limited to PDF/JPG/PNG and `maxSize = MAX_FILE_BYTES`; dashed border (muted; accent on drag-over), min-height 96px, instruction "Drag and drop a file here, or click to browse" and hint "PDF, JPG, or PNG — up to 10 MB" (exact UI-SPEC copy). On drop: upload to `event-files` bucket at `{eventId}/{uuid}-{filename}` via the browser client, then call `saveFilePath` with metadata + optional tag (checklist item or supplier label, "Tag to" control); reject wrong-type/too-large client-side with the exact Global Error State toasts. Create app/(client)/event/files/page.tsx (Server Component): `assertRole('client')`, list event_files; empty state exact copy "No files uploaded yet" + body + "Upload File"; else render FileDropzone + FileGrid. FileGrid.tsx: 3-col desktop / 1-col mobile. FileCard.tsx: file-type icon + truncated filename + tag + upload date + download (calls getSignedUrl then opens the URL) + delete (confirm Dialog with exact UI-SPEC delete copy). Icon-only buttons get aria-labels ("Download file", "Delete file"). sonner toasts. Weights 400/600, stone/accent palette.
  </action>
  <verify>
    <automated>grep -q "Drag and drop a file here" components/files/FileDropzone.tsx && grep -q "assertRole" app/\(client\)/event/files/page.tsx && grep -q "getSignedUrl" components/files/FileCard.tsx && grep -rq "aria-label" components/files/ && npx tsc --noEmit && echo FILES_UI_OK</automated>
  </verify>
  <acceptance_criteria>
    - Dropzone restricts to PDF/JPG/PNG and 10MB and uses the exact UI-SPEC instruction + hint copy
    - Empty state uses exact copy "No files uploaded yet"
    - FileCard download uses `getSignedUrl`; delete uses the exact UI-SPEC confirmation copy
    - Page calls `assertRole('client')`; icon-only buttons have aria-labels
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Files page renders upload (with tagging + type/size guards), grid, signed-URL download, and delete with locked copy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client upload → event-files bucket | Untrusted file content + mime crosses here |
| download request → signed URL | Time-limited access to private objects |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-11 | Tampering | Malicious / wrong-type upload | mitigate | react-dropzone `accept` + `maxSize` client-side AND `saveFilePath` server-side mime re-check against ACCEPTED_MIME; DB CHECK on event_files.mime_type |
| T-1-01 | Information Disclosure | Cross-event file access | mitigate | event-files bucket private; storage RLS gates on event-id folder; `getSignedUrl` row lookup is RLS-scoped; short 60s signed URLs |
| T-1-04 | Information Disclosure | Public bucket leak | mitigate | Bucket created `public = false` (Plan 01-02); downloads only via signed URL |
</threat_model>

<verification>
- `npx vitest run tests/schemas/file.test.ts` green
- Dropzone restricts types/size; download via signed URL; actions call verifySession
- `npx tsc --noEmit` exits 0
</verification>

<success_criteria>
- ROADMAP Success Criterion 4 satisfied: upload PDF/JPG/PNG tagged to supplier/checklist item or general, download anytime
- MIME validated client + server; storage private with signed-URL downloads
- Files copy matches UI-SPEC
</success_criteria>

<output>
Create `.planning/phases/01-foundation-planning-tools/01-07-SUMMARY.md` when done.
</output>
